<h1>Add new image for <%= @equipment.name %></h1>

<%= form_for([@equipment, @equipment_image]) do |f| %>
  <div class="form-group">
    <%= f.label :image %>
    <br />
    <%= f.file_field :image, accept: "image/*" %>
  </div>
  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>